= simple_form_for [:admin, @newsitem], defaults: { input_html: { class: 'form-control' } } do |f|
  = f.input :title, input_html: { autofocus: true }
  = f.input :content, input_html: { rows: 10 }
  = f.submit class: 'btn btn-default'
  = link_to 'Cancel', admin_newsitems_path, class: 'btn btn-sm'
