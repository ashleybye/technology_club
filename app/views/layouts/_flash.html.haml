- flash.each do |key, value|
  .container
    .row
      .col-xs-12
        %div{:class => flash_css_class(key.to_sym)}
          %button.close( data-dismiss="alert" type="button" )
            %span( aria-hidden="true" ) &times;
            %span.sr-only Close
          = value.html_safe
